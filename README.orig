<<<<<<< HEAD
HowTo set: 
  $ cd ~
  $ git clone https://github.com/snaiffer/bash_env.git bash_env
) for one user in interactive a login shell (Ordinary terminal)
  $ echo "" > .bash_profile && sed -i 1i"source $PWD/bash_env/bash_env" .bash_profile
) for all users in interactive login shell:
  $ sudo echo "" > /etc/profile && sudo sed -i 1i"source $PWD/bash_env/bash_env" /etc/profile
) for one user in interactive not a login shell (Terminals in X server)
  $ echo "" > .bashrc && sed -i 1i"source $PWD/bash_env/bash_env" .bashrc
=======
HowTo set:
  $ cd ~
  $ git clone https://github.com/snaiffer/bashrc.git bashrc
  $ echo "" > .bashrc && sed -i 1i"source $PWD/bashrc/bashrc" .bashrc
>>>>>>> 0859d0090d6aaf81d04624817ccd3e39fea57757
